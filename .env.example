#!/bin/bash
# Example environment configuration for modernized server.js
# Copy to .env or source before running: source .env.example

# Server Configuration
export PORT=3000
export DASHBOARD_PASSWORD="changeme123"

# CORS Configuration (NEW)
# Comma-separated list of allowed origins for public deployment
# Leave unset for LAN-only mode (default: 192.168.x.x, 10.x.x.x, etc.)
# Examples:
# export ALLOWED_ORIGINS="https://media.mydomain.com"
# export ALLOWED_ORIGINS="https://media.mydomain.com,https://ingest.example.com"
# export ALLOWED_ORIGINS="https://*.mydomain.com"
export ALLOWED_ORIGINS=""

# Jellyfin Integration (NEW)
# Configure to enable real API-based library scanning
# Get API key from: Jellyfin Dashboard > API Keys
export JELLYFIN_URL="http://localhost:8096"
export JELLYFIN_API_KEY=""

# Optional: Debug mode
# export DEBUG=1

# Start the server
# node server.js
